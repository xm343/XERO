    <!-- Footer -->
    <style>
        /* Footer */
        .footer {
            background: #000;
            color: #fff;
            padding: 80px 0 40px;
            margin-top: 100px;
        }
        .footer-logo {
            height: 35px;
            margin-bottom: 25px;
            filter: brightness(0) invert(1);
        }
        .footer h6 {
            font-weight: 800;
            text-transform: uppercase;
            font-size: 14px;
            margin-bottom: 25px;
            color: #fff;
        }
        .footer ul {
            padding: 0;
            list-style: none;
        }
        .footer ul li a {
            color: #ccc;
            text-decoration: none;
            font-size: 14px;
            transition: 0.3s;
        }
        .footer ul li a:hover {
            color: #fff;
        }
        .footer p {
            color: #ccc;
        }
        .footer .text-muted {
            color: #aaa !important;
        }
    </style>
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 mb-5">
                    <img src="/images/xero-logo.png" alt="XERO" class="footer-logo">
                    <p class="text-muted small">Redefining streetwear with premium quality and bold designs. Join the movement. Made for the Misfits.</p>
                </div>
                <div class="col-lg-2 col-md-4 mb-4">
                    <h6>Explore</h6>
                    <ul>
                        <li><a href="/">Home</a></li>
                        <li><a href="/shop">Shop All</a></li>
                    </ul>
                </div>
                <div class="col-lg-2 col-md-4 mb-4">
                    <h6>Support</h6>
                    <ul>
                        <li><a href="#">Shipping</a></li>
                        <li><a href="#">Returns</a></li>
                    </ul>
                </div>
                <div class="col-lg-4 col-md-4 mb-4">
                    <h6>Newsletter</h6>
                    <div class="input-group mb-3">
                        <input type="text" class="form-control bg-transparent border-secondary text-white rounded-0" placeholder="Enter your email">
                        <button class="btn btn-outline-light rounded-0" type="button">JOIN</button>
                    </div>
                </div>
            </div>
            <div class="text-center pt-5 border-top border-secondary mt-5">
                <p class="text-muted small mb-0">&copy; 2024 XERO Apparel. All Rights Reserved.</p>
            </div>
        </div>
    </footer>

    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        async function addToWishlist(productId, element) {
            try {
                const response = await fetch('/addToWishlist', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ productId })
                });

                const data = await response.json();

                if (data.status === true) {
                    Swal.fire({
                        title: 'Added to Wishlist',
                        text: 'The product has been added to your wishlist',
                        icon: 'success',
                        showConfirmButton: false,
                        timer: 1500
                    });
                    
                    if (element) {
                        const icon = element.querySelector('i');
                        if (icon) {
                            icon.classList.remove('fa-regular');
                            icon.classList.add('fa-solid');
                            icon.style.color = 'red';
                        }
                    }
                } else {
                    if (data.message === 'Product already in wishlist') {
                        if (element) {
                            const icon = element.querySelector('i');
                            if (icon) {
                                icon.classList.remove('fa-regular');
                                icon.classList.add('fa-solid');
                                icon.style.color = 'red';
                            }
                        }
                    }
                    
                    Swal.fire({
                        title: data.message === 'Please login to add to wishlist' ? 'Login Required' : 'Already in Wishlist',
                        text: data.message,
                        icon: 'info',
                        showConfirmButton: false,
                        timer: 1500
                    });
                }
            } catch (error) {
                Swal.fire({
                    title: 'Error',
                    text: 'Something went wrong',
                    icon: 'error',
                    showConfirmButton: false,
                    timer: 1500
                });
            }
        }

        async function addToCart(productId) {
            try {
                const response = await fetch('/addToCart', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ productId, quantity: 1 })
                });
                
                const data = await response.json();
                
                if (data.status === true) {
                    Swal.fire({
                        title: 'Added to Cart',
                        text: 'Product added to cart successfully',
                        icon: 'success',
                        showConfirmButton: false,
                        timer: 1500
                    });
                } else {
                    Swal.fire({
                        title: 'Error',
                        text: data.message || 'Failed to add to cart',
                        icon: 'error',
                        showConfirmButton: false,
                        timer: 1500
                    });
                }
            } catch (error) {
                console.error('Error adding to cart:', error);
                Swal.fire({
                    title: 'Error',
                    text: 'Something went wrong',
                    icon: 'error',
                    showConfirmButton: false,
                    timer: 1500
                });
            }
        }
    </script>
</body>
</html>
